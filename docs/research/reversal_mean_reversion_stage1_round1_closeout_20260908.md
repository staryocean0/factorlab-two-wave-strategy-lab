# Broad reversal / mean-reversion Stage-1 round-1 closeout — 2026-09-08

Program identity: `broad_reversal_mean_reversion_discovery_program_v1`

Decision:

`BROAD_RMR_STAGE1_ROUND1_CLOSED_NO_PROMOTED_MECHANISM`

## Why this round closes now

The repository-wide authority reset changed this project from a single two-wave recognizer optimizer into a broad reversal / mean-reversion direction finder. Round 1 has now given bounded, preregistered budgets to four distinct mechanism classes on the same consumed 2015–2020 evidence surface:

1. R1 cross-scale pullback;
2. R2 range-boundary / failed-breakout reversion;
3. R3 structural exhaustion / transition;
4. R4 statistical-state extremes.

R1 also received one results-blind measurement revision after its original complete-finer-two-wave event definition proved too sparse. No further automatic repair budget remains on the same consumed window.

Continuing to invent R5/R6/R7 state variables now would raise multiplicity without a new data/theory boundary and would violate the direction-finder design.

## Evidence map

### M0 — two-wave structure foundation

Status remains:

`morphology_replication_not_yet_accepted`

The historical two-wave stack remains valuable measurement infrastructure. v0.6.17 authoritative-source formal replay (`CL-20260908-005`) is still a separate legitimate M0 task and is not closed by this strategy-screen result.

### R1 — cross-scale pullback

Final status:

`unresolved_evidence_insufficient_after_one_measurement_revision`

R1 v1 complete-finer-two-wave event supply was far too sparse. M1 replaced the lower event with a results-blind single counter-shock at `0.5 × parent amplitude` while keeping parent L5 / 5m / structural boundaries frozen.

M1 trigger supply passed, but resolved first-passage check supply did not:

- BUILD 168;
- 2019 40;
- 2020 39.

Because the frozen annual minimum was 50, R1 cannot be called rejected. It remains unresolved.

Do not lower the gate or create another measurement revision on the same 2015–2020 outcomes. A future revisit requires materially new data or an independently theory-driven new event identity approved before outcomes.

### R2 — range-boundary reversion

Final status:

`closed_with_adequate_evidence`

M1 resolved supply was large (657 / 161 / 151), but parent range state worsened the simple excursion-severity baseline in pooled 2019–2020 and in both individual years.

No same-identity rescue tuning is allowed.

### R3 — structural exhaustion / transition

Final status:

`closed_predeclared_direction_falsified`

Supply was ample (744 / 172 / 144). Both simple deterioration variables produced coefficients opposite the preregistered hypothesis direction despite tiny probability-metric gains.

The sign cannot be reinterpreted post hoc and the identity cannot be rescued with HMM/Koopman/deep regimes.

### R4 — statistical-state extremes

Final status:

`closed_no_candidate_qualifies`

All three independent candidates had ample resolved supply and all worsened the same geometry baseline:

- path inefficiency;
- lower-scale event density;
- parent amplitude extremity.

No state combination, window search, threshold search or deep rescue is authorized.

## Round-1 scientific conclusions

### 1. Parent structure is a useful coordinate, not automatically alpha

The M0 two-wave stack provides a disciplined causal language for parent structure, but the existence of a clean parent state does not imply a simple reversal edge.

### 2. Event-supply failure and mechanism failure are different

R1 remains unresolved because its first-passage resolved annual check sample is insufficient. R2/R3/R4 are genuine low-capacity mechanism failures because their evidence supply is adequate.

### 3. Statistical-state behavior must not be confused with price mean reversion

R4 explicitly tested path efficiency, event density and amplitude-state extremity. Their own time-series behavior did not translate into better parent-failure versus extension probabilities.

Therefore:

> a state statistic being persistent, extreme or mean-reverting is not sufficient evidence of a tradable price-reversion mechanism.

### 4. The broad program should stop automatic feature invention here

No mechanism was promoted. That is an acceptable scientific result.

Do not use the same 2015–2020 evidence to keep generating nearby indicators until one passes.

## What can reopen broad discovery

A new broad mechanism screen may open only after one of these happens:

1. **materially new data** becomes available and its BUILD/check/holdout roles are frozen before outcome inspection;
2. an **independently theory/literature-driven mechanism** is specified before consulting these round-1 outcome details and receives a new program review;
3. M0 itself produces a materially new accepted measurement object that changes the available causal state representation, followed by a new results-blind program charter amendment.

These conditions do not authorize recycling R2/R3/R4 unchanged.

## Immediate next work

While broad strategy discovery is paused:

- continue the already-frozen M0 v0.6.17 authoritative-source formal replay when the local authoritative DataHub execution result is available;
- preserve all round-1 receipts and failures;
- prepare to admit future unified-data updates without opening them before a new role freeze;
- do not perform PnL optimization, paper trading or production work.

Production authority remains `false`.
