# C0 → C1：图形失败归因后的震荡冲突修正

前置频段比较与C0定义在94b9fb5完整执行；C0源码和所有旧输出保留。该修正不改变频段ER公式/统计，也不搜索阈值。

实际审阅主5m/.01预选16个图后，case_05_C_new_range（五点40408、40421、40461、40498、40548）显示高点先上后下、低点下降，原C0仅据D接近零和相位总跨度小于0.5个宽度判为range。这与原需求中避免‘一浪上移另一浪下移但总体接近零’的约束不一致。

C1仅对C0的range增加两类拒判：同相位两个周期位移明显相反，或多数相位累计位移与另一相位位移明显相反。明确程度沿用原0.15个拟合宽度容差，不新调门槛；原C0趋势必须三个位移同向的条件不变。被修正对象进入uncertain，不能为了增加覆盖改成其他明确方向。

C1保留源C0记录ID、原分类、两类冲突、独立配置hash和新schema；所有变化于原结构确认时产生，使用当时已知完整两周期数据。仍单列A通道适用性，不产生交易权限。

追加5项机制/前缀测试，本地30项新测试通过。首个本地C1执行因整理运行脚本而主动终止，部分产物不作为完成证据；随后使用整理完毕且哈希冻结的运行器重跑，并在Python3.11远端执行完整测试和六视图三尺度重放。完成状态以运行结果为准。

C0预选图还暴露跳跃/平台主导的方向趋势和门槛附近弱方向；它们不通过此修正被自动解决。图形自审是开发证据，不是独立人类真值。主目标5m，一分钟依然只是诊断对照。
