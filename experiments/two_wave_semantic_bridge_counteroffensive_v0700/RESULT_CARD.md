# v0.7.0 Semantic-Bridge Counteroffensive / Salvage Staircase — Result Card

Formal workflow run: `34671433841`  
Protocol: `docs/research/TWO_WAVE_SEMANTIC_BRIDGE_COUNTEROFFENSIVE_V0700_PROTOCOL.md`  
Protocol freeze commit: `525948dc7f22a01fdfd6df453a935181df41e0f6`  
Formal result commit: `4c0fc9366dd8e2ce6537f9c384c24d90f2d2f920`

## Counteroffensive question

The audit does not ask whether all historical v0.6.x work was wrong. It asks where the old causal infrastructure first loses the independently labeled complete-same-scale-two-wave parent semantics, so later components can be transplanted rather than discarded wholesale.

Primary discovery universe: the 11 frozen v0.6.48 reference-positive candidate cases with complete final-reference p0..p4 anchors. The audit uses no notes/confidence, direction prediction, future outcome, PnL, or fitted bar tolerance.

## Salvage staircase

Development-level salvage threshold was frozen at `8/11` cases.

| Layer | Meaning | Support |
|---|---|---:|
| L0 | observed raw-turn support in human cells | `11/11 = 100%` |
| L1 | one common TCSS/ridge scale supports all five human cells | `10/11 = 90.91%` |
| L2 | an exact-ridge five-tuple supports the five human cells | `3/11 = 27.27%` |
| L3 | a causal exact-ridge tuple birth supports them by cutoff | `3/11 = 27.27%` |
| L4 | legacy canonical filtered identity supports them | `2/11 = 18.18%` |
| L5 | legacy published raw identity supports them | `3/11 = 27.27%` |

The decisive transition is **L1 → L2**: `7/11` cases have the required same-scale ridge-node support but fail the requirement that those five semantically relevant ridge nodes form one legacy exact consecutive five-ridge tuple.

There is **no L2 → L3 loss**: every human-compatible exact tuple found by cutoff also appears as a causally certified tuple birth. Therefore the child-death / birth timing rule is not the first failure point.

## Ridge infrastructure salvage

L1 is strongly supported:

- 10 of 11 anchored human-positive cases have all five target cells represented at one or more common TCSS levels;
- median number of supporting common levels is `6`;
- for those cases, nearest compatible common-scale ridge-node distance has median `0` bars for every ordinal;
- maximum nearest distance is only `1` bar for p0-p3 and `4` bars for p4.

Therefore TCSS extrema and ridge lineage are retained as **support infrastructure**, but not as semantic-parent authority.

## Exact tuple objectization failure

The old v0.5.2 semantic bridge requires five alternating ridge nodes to become one exact consecutive five-ridge tuple. That requirement retains only `3/11` of the independently confirmed parent objects despite L1 supporting `10/11`.

Formal verdict:

**`v0700_ridge_infrastructure_salvage_supported_exact_tuple_objectization_breaks_semantic_bridge`**

This localizes the first major semantic break to **exact five-ridge tuple objectization**, not to source data, causal extrema detection, ridge tracking, tuple-birth confirmation, ordinal-0 publication mechanics, qualification thresholds, or direction classification.

## Component retention map

- data / clock / source hashes / causal replay / governance: **retain without semantic change**;
- TCSS extrema / ridge lineage: **retain as support infrastructure, not parent authority**;
- exact-ridge five-tuple objectization: **reconstruct or revalidate**;
- tuple-birth causal adjacency rule: **retest after repaired objectization**; current evidence does not identify it as the first failure;
- legacy filtered identity: **reconstruct or revalidate downstream of repaired objectization**;
- v0.6.4/v0.6.5 raw projection/publication: **historical component awaiting transplantation test**;
- v0.5.4 / v0.6.18 same-scale qualification: **historical component awaiting transplantation test**;
- D1 / v0.6.25 direction: **historical component awaiting transplantation test**.

No downstream component is declared false solely because its historical upstream object was mis-specified.

## Next authorized move

Start **v0.7.1 ridge-supported semantic parent objectization reconstruction**.

The new work must use the retained TCSS/ridge state space and reconstruct the parent-object bridge without requiring the five semantically relevant ridges to be exactly consecutive at one scale. It must not fit a bar-distance tolerance to these 11 labels. Candidate object families should be defined structurally first, then compared on Development reference evidence; downstream raw projection, qualification, and direction are reattached one layer at a time only after a semantic object constructor is selected.

Authority remains unchanged:

- `morphology_acceptance=false`
- `trade_authority=false`
- `production_authority=false`
- parent-direction winner remains unset.
