# v0.7.2 F3 Causal Event / Publication Transplant — Result Card

Formal workflow run: `34679677705`  
Protocol: `docs/research/TWO_WAVE_F3_CAUSAL_EVENT_PUBLICATION_TRANSPLANT_V0702_PROTOCOL.md`  
Protocol freeze commit: `3e4e12d27dc28f93aa3079d8ea0899601931ec60`  
Formal result commit: `580828754920f5c9a747bee310c0657bf41443c3`

## Purpose

Test whether the v0.7.1 F3 persistence-dominant static reconstruction can be converted into a permanently causal, immutable event without losing the frozen `8/11` Development semantic support, then precheck whether historical raw-projection/publication/qualification/direction interfaces can be transplanted without retuning.

## Frozen semantic-preservation results

The v0.7.1 static lineage reproduces exactly:

- static F3 support: **`8/11`**

After requiring explicit skipped-ridge death certificates and selected-boundary survival beyond each skipped ridge's death:

- causally certified F3 support: **`7/11`**
- frozen minimum required: **`8/11`**

Therefore the current F3 static objectization does **not** yet have an adequate permanent causal event certificate under the preregistered rule.

The failure is narrow: exactly one of the eight static-supported Development cases loses support at causal event certification.

## Raw projection / immutable publication evidence

The frozen nonconsecutive-predecessor transplant of the v0.6.4 sequential raw-projection principle produced:

- first-valid published raw semantic support: **`9/11`**
- per-ordinal published-raw cell-hit cases:
  - ordinal 0: `11/11`
  - ordinal 1: `11/11`
  - ordinal 2: `11/11`
  - ordinal 3: `11/11`
  - ordinal 4: `10/11`
- prior-invalid projections: `0`
- later-valid same-identity confirmations: `2378`
- suppressed would-be rewrites: `223`

The publication result does **not** override the earlier causal-event failure because the frozen decision precedence stops at the first unsupported layer. It is nevertheless positive salvage evidence for the historical sequential raw-projection and first-valid immutable-publication principles once a valid causal F3 event object is available.

Event-certification delay across certified realizations has median `0` bars and maximum `25` bars.

## Downstream interface precheck

Unique published F3 raw records entering the no-reference interface precheck: `1478`.

Observed diagnostics:

- interface exceptions: `461`, all `ValueError`
- v0.6.18 qualified: `89 / 1478 = 6.02%`
- D1 state counts on successful interface records:
  - downtrend `240`
  - range `2`
  - uncertain `1035`
  - uptrend `201`
- v0.6.25 state counts:
  - downtrend `400`
  - range `5`
  - uncertain `271`
  - uptrend `341`
- **D1 decisive overrides by v0.6.25: `0`**

No reference scoring was performed here. Because the causal-event gate fails earlier, interface exceptions and qualification/state distributions are descriptive transplant diagnostics, not the primary verdict and not semantic-performance evidence.

## Formal verdict

**`v0702_f3_static_objectization_not_causally_publishable`**

Interpretation:

- TCSS/ridge infrastructure remains salvaged.
- F3 static persistence-dominant objectization remains a supported Development reconstruction candidate.
- The current permanent death-certificate event rule is one case short of preserving the frozen F3 semantic support and therefore is **not** promoted.
- The v0.6.4 sequential raw-projection principle and v0.6.5 first-valid immutable-publication principle receive positive transplant evidence, but cannot become active until a causal F3 event is established.
- v0.5.4/v0.6.18 qualification and D1/v0.6.25 direction remain historical components awaiting later semantic transplantation tests; they are not declared false.

## Next authorized step

Freeze a read-only **causal-certification gap attribution** focused only on the one `static-supported / causal-unsupported` F3 case and aggregate event mechanics. Determine whether the loss is caused by:

1. missing explicit death record for a skipped ridge;
2. selected boundary ridge lacking a coarse-level representation needed by the current proof;
3. death/survivor confirmation occurring after the frozen cutoff;
4. an overly strong certificate formulation that requires evidence not logically necessary for permanence.

Do **not** lower the `8/11` gate, use future survival, fit event-delay tolerance, change F3 persistence dominance, or move ahead to qualification semantic calibration before causal event semantics are resolved.

`morphology_acceptance=false`, `trade_authority=false`, `production_authority=false`.
