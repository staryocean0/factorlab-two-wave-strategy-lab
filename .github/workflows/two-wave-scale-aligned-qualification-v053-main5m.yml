# Research logic is frozen in the result-before protocol; this update only triggers the existing workflow.
name: two-wave-scale-aligned-qualification-v053-main5m
on:
  push:
    branches: ['codex/two-wave-phase1-20260905']
    paths:
      - 'src/factor_lab/visual_structure/two_wave/scale_aligned_qualification_v053.py'
      - 'tests/unit/test_two_wave_scale_aligned_qualification_v053.py'
      - 'scripts/run_two_wave_scale_aligned_qualification_v053.py'
      - 'docs/research/two_wave_scale_aligned_qualification_protocol_v053.md'
      - '.github/workflows/two-wave-scale-aligned-qualification-v053-main5m.yml'
  workflow_dispatch:
permissions:
  contents: read
  actions: read
concurrency:
  group: two-wave-scale-aligned-qualification-v053-main5m-${{ github.ref }}
  cancel-in-progress: false
defaults:
  run:
    shell: bash
jobs:
  synthetic-and-main5m-mechanism:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip
      - run: python -m pip install -e . editables==0.6
      - name: Validate bounded package and frozen data
        run: |
          mkdir -p v053_main5m_proof/source
          python scripts/validate_theme_package.py | tee v053_main5m_proof/package_validation.json
          git rev-parse HEAD > v053_main5m_proof/commit.txt
      - name: Full regression before v0.5.3 real-data result
        run: python -m pytest -q --junitxml=v053_main5m_proof/tests.xml | tee v053_main5m_proof/tests.log
      - name: Run frozen v0.5.3 main 5m mechanism audit
        run: |
          python scripts/run_two_wave_scale_aligned_qualification_v053.py | tee v053_main5m_proof/research.log
          git diff --exit-code -- data src tests scripts docs/research pyproject.toml
      - name: Retain exact implementation and environment
        if: always()
        run: |
          python -m pip freeze > v053_main5m_proof/environment.txt
          cp src/factor_lab/visual_structure/two_wave/scale_aligned_qualification_v053.py v053_main5m_proof/source/
          cp tests/unit/test_two_wave_scale_aligned_qualification_v053.py scripts/run_two_wave_scale_aligned_qualification_v053.py docs/research/two_wave_scale_aligned_qualification_protocol_v053.md v053_main5m_proof/source/
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: two-wave-scale-aligned-qualification-v053-main5m-${{ github.sha }}
          path: |
            cloud_results/two_wave_scale_aligned_qualification_v053_main5m/
            v053_main5m_proof/
          retention-days: 30
          if-no-files-found: error
