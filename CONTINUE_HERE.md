# 两浪 H0：自主迭代入口（2026-09-05，首轮已运行并审计）

## 最新状态：不是等待用户逐版本盲审

用户明确要求由 AI 自主完成实现、历史重放、图形归因、自审和版本迭代，候选成熟后才集中交给用户验收。没有真人标签不阻止研发；不得把 AI 自审包装为独立人类真值。本轮已实际完成首轮，而不是只写计划或启动工作流。

优先读 [本轮自主迭代结果与图形归因](docs/research/two_wave_autonomous_results_20260905.md)，然后读 [预定协议](docs/research/two_wave_autonomous_iteration_v02.md) 和 [精简执行证据](cloud_results/two_wave_candidate_v02_delivery/execution_evidence.json)。

## 已执行的研究代码

- 起点 `46273ddace21769a7f73d8d8b627d77dbeb43e22`；实际测试/checkout 代码 `be3aea5f5313855cf6ce94c804fe95f7e33f9311`。
- 自主全量运行 `33952622936` / job `101270115594` 成功；228 项测试通过，473 原资产、14 产品、749,337 源行、15 工具通过验证。
- 14 视图 × 3 尺度 × 3 方法 = 126 次主重放。42 组几何/成员/时钟对齐通过；225 次固定真实前缀独立重放通过；578,895 条实际发出事件核对。没有声称每个真实前缀都独立重放过。
- 相同 43,874 候选，旧版明确 2,187，A 明确 2,361，B 明确 5,620。A 新明确 175、反向新增拒判 1；不是准确率。
- 新旧原生工件都已完整下载且 SHA-256 核验；旧版 168 文件、324,881 条解析记录完全一致，序列化字节不同。
- 另一个不导入候选模块的计算核对器在 4,845 个旧前缀结构上核验三方法 14,535 条记录，无差异。是独立实现核对，不是独立真人参考。
- 已检查 13 张全量原生图和 5 张旧前缀诊断图。完整原生工件 ID `9965431872`，107,707,450 字节，2026-10-05 07:35:47 UTC 到期，见结果报告的完整 SHA-256。

当前收尾提交增加报告、执行证据、独立工件核对脚本，不改变已运行的候选识别器/原工作流；不能冒充再次运行了远端全仓库测试。工件核对脚本已在下载工件上本地实际执行。

## 下一轮由 AI 独立推进的具体问题

A 改用完整周期去共同漂移后的宽度，纠正原始极差混入漂移/时长；B 额外将速度差降为属性。保留 A 为下一步基线候选、B 为必要对照，未按覆盖率直接选完成品，也未替换 v0.1。

重点是把方向趋势与通道形态/置信程度分开诊断：case_07 整体高低点上涨但通道收敛，被 A/B 继续单独拒判；case_10 仍有向上漂移，但新宽度比 1.7453 使旧上涨转为拒判。B 还包含 358 个微小反向位移（均在原 0.15 容差内）和 288 个靠近 E=0.2 门槛的明确对象。不要未经归因一律判错，也不要忽略。

在 .01 的统一微扰下，13,839/14,155 精确匹配，原始316个未匹配；v01/A/B 分别3/3/4次匹配分类变化，54次确认bar变化、88次首越界时间或方向变化。未匹配对象不能丢弃，不能据此宣布全面稳定。三方法所有对象中各有3,527个在确认时已越界，不能回填成提前可交易。

为少量新候选先记录定义、反例与比较规则，再重放并检查退化；继续保存成功、失败及边界图。当前不是 ready_for_user_review，但不能把研发停在收真人标签上。

## 文件与不变边界

- 候选实现：`src/factor_lab/visual_structure/two_wave/candidate_v02.py`
- 重放审计：`scripts/run_two_wave_candidate_audit.py`
- 独立工件核对：`scripts/audit_two_wave_candidate_reference_oracle.py`
- 工作流：`.github/workflows/two-wave-candidate-audit.yml`
- 历史基线证据：[旧同步报告](docs/research/two_wave_sync_20260905.md)。旧等待盲标的开发前置顺序已被用户更新，旧实验事实保留。

原 v0.1 识别规范、评价协议、geometry/models/engine/data 等既有源码、原十五工具及数据原字节保留。不重采样，不获取或补造2021年以后的数据，不把2015—2020开发期改称新样本外。H1/H2、收益择优、交易与生产注册未开放。用户本级唯一策略性退出仍是反向突破，同向突破不自动退出或交接。

原分支写回和研究运行授权继续有效，保留私有和草稿PR，不合并main、不强推。后续写回先核验最新head并保留他人变更；结果始终归属实际执行提交。只有成熟候选才集中交给用户验收，不要求用户逐版审图。
